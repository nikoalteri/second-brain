---
phase: 19-revolving-credit-card-interest-engine-correctness-align-cycl
plan: 04
type: execute
wave: 3
depends_on: [19-03]
files_modified:
  - app/Services/RevolvingCreditCalculator.php
  - app/Enums/InterestCalculationMethod.php
  - tests/Unit/RevolvingCreditCalculatorTest.php
  - tests/Unit/CreditCardDailyBalanceTest.php
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "Selecting the direct_monthly interest method charges one twelfth of the annual rate per month, not the full annual rate"
    - "The enum's own label/description text describes the corrected formula"
    - "No test in the suite still asserts the old ~12x-too-high figure as correct"
    - "The direct_monthly enum value stays a valid, documented API option"
  artifacts:
    - path: "app/Services/RevolvingCreditCalculator.php"
      provides: "Flat monthly rate in calculateInterestDirectMonthly"
      contains: "annualRatePercent / 100 / 12"
    - path: "app/Enums/InterestCalculationMethod.php"
      provides: "Description matching the corrected formula"
      contains: "/ 12"
    - path: "tests/Unit/RevolvingCreditCalculatorTest.php"
      provides: "Corrected direct_monthly assertions with synthetic fixtures"
      contains: "calculateInterestDirectMonthly"
  key_links:
    - from: "app/Services/RevolvingCreditCalculator.php::calculatePaymentBreakdown"
      to: "calculateInterestDirectMonthly"
      via: "InterestCalculationMethod::DIRECT_MONTHLY branch (unchanged wiring, corrected math)"
      pattern: "InterestCalculationMethod::DIRECT_MONTHLY"
---

<objective>
Fix the fourth D-01 defect: `calculateInterestDirectMonthly()` applies the full annual rate as one month's charge (~12x too high). Replace it with a flat monthly approximation `annualRatePercent / 100 / 12`, update the enum's description text, and correct every existing test assertion that currently pins the wrong figure as "expected".

Purpose: `direct_monthly` is not the default, but it is a selectable, API-documented option — a card configured with it today would be billed roughly twelve times the correct interest. The fix keeps the enum's public surface (validation rules, Scribe docs, Filament label) intact while removing the landmine.
Output: Corrected formula, corrected enum description, and four corrected test methods across two files with all real-statement figures and provenance comments stripped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-revolving-credit-card-interest-engine-correctness-align-cycl/19-CONTEXT.md
@.planning/phases/19-revolving-credit-card-interest-engine-correctness-align-cycl/19-RESEARCH.md
@.planning/phases/19-revolving-credit-card-interest-engine-correctness-align-cycl/19-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

Current (buggy) method — app/Services/RevolvingCreditCalculator.php:109-128:
```php
/**
 * Calculate total interest using direct monthly method
 *
 * Interest = current_balance × (annual_rate / 100)
 * This applies the annual rate directly as a monthly charge
 */
public function calculateInterestDirectMonthly(float $currentBalance, float $annualRatePercent): float
{
    if ($currentBalance <= 0 || $annualRatePercent <= 0) {
        return 0.0;
    }

    return round($currentBalance * ($annualRatePercent / 100), 2);
}
```

Current enum text — app/Enums/InterestCalculationMethod.php:29-35:
```php
public function getDescription(): string
{
    return match ($this) {
        self::DAILY_BALANCE  => 'Interest = Σ(daily_balance × rate/365) per each day',
        self::DIRECT_MONTHLY => 'Interest = balance × (annual_rate / 100) applied directly each month',
    };
}
```

Call site (unchanged by this plan) — app/Services/RevolvingCreditCalculator.php:177-179:
```php
if ($method === InterestCalculationMethod::DIRECT_MONTHLY) {
    $interestAmount = $this->calculateInterestDirectMonthly($currentDebt, $annualRate);
}
```

API validation that must keep accepting the value (do NOT change):
```php
// StoreCreditCardRequest.php:36 / UpdateCreditCardRequest.php:37
Rule::in(['daily_balance', 'direct_monthly'])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the direct-monthly formula with a flat twelfth of the annual rate</name>
  <files>app/Services/RevolvingCreditCalculator.php, app/Enums/InterestCalculationMethod.php</files>
  <read_first>
    - app/Services/RevolvingCreditCalculator.php (`calculateInterestDirectMonthly` lines 109-128 and its call site inside `calculatePaymentBreakdown` around line 177)
    - app/Enums/InterestCalculationMethod.php (whole file — `getLabel`, `getColor`, `getDescription`)
    - app/Services/CreditCardCycleService.php (confirm plan 19-02 already applied the same `/ 12` correction to the legacy `calculateRevolvingPaymentBreakdown`, so both sites now agree)
  </read_first>
  <behavior>
    - `calculateInterestDirectMonthly(600.00, 14.0)` returns exactly `7.0`   (600 × 14 / 100 / 12 = 7.00)
    - `calculateInterestDirectMonthly(1000.00, 12.0)` returns exactly `10.0`
    - `calculateInterestDirectMonthly(0.0, 14.0)` returns `0.0`
    - `calculateInterestDirectMonthly(600.00, 0.0)` returns `0.0`
    - `calculateInterestDirectMonthly(-50.0, 14.0)` returns `0.0`
  </behavior>
  <action>
`tests/Unit/RevolvingCreditCalculatorTest.php` currently has NO `use PHPUnit\Framework\Attributes\Test;` import despite every method being annotated with `#[Test]` — PHPUnit silently discovers zero tests in this file today (`php artisan test --filter=RevolvingCreditCalculatorTest` reports "No tests found"). Add `use PHPUnit\Framework\Attributes\Test;` to this file's import block FIRST, before making any other edit in this task or Task 2 — otherwise every verify step in this plan runs against a file PHPUnit can't see, and "0 failures" would mean "0 tests ran," not "all tests pass." (This mirrors the same fix 19-03-PLAN.md Task 1 already applies to the sibling file `CreditCardDailyBalanceTest.php`.)

In `app/Services/RevolvingCreditCalculator.php`, change the return statement of `calculateInterestDirectMonthly()` from:

```php
return round($currentBalance * ($annualRatePercent / 100), 2);
```

to:

```php
return round($currentBalance * ($annualRatePercent / 100 / 12), 2);
```

Use the flat `/ 12` split specifically — do NOT introduce a compound monthly-equivalent such as `(1 + r/365)^(365/12) - 1`. The enum's own contract is "applied directly each month", and a flat twelfth preserves that simplified/direct intent (the day-precise alternative is what `daily_balance` is for) while removing the ~12x error. No compound-interest math is being introduced here.

Keep the `if ($currentBalance <= 0 || $annualRatePercent <= 0) return 0.0;` guard unchanged.

Replace the method's docblock body with:
```php
/**
 * Calculate total interest using the direct monthly method.
 *
 * Interest = current_balance × (annual_rate / 100 / 12)
 *
 * The stored rate is the ANNUAL nominal rate (TAN); this method charges a flat twelfth of
 * it per month. It is a deliberate simplification — use DAILY_BALANCE for day-precise
 * accrual. Before Phase 19 this applied the FULL annual rate every month (~12x too high).
 */
```

In `app/Enums/InterestCalculationMethod.php`, change the `DIRECT_MONTHLY` line of `getDescription()` to:
```php
self::DIRECT_MONTHLY => 'Interest = balance × (annual_rate / 100 / 12) — a flat twelfth of the annual rate, applied directly each month',
```
Leave `getLabel()` and `getColor()` unchanged, and leave both enum cases in place — `direct_monthly` remains a valid, API-documented option (`Rule::in(['daily_balance', 'direct_monthly'])` in both request classes and the Scribe docs stay untouched).

Add the five `<behavior>` assertions as one `#[Test]` method in `tests/Unit/RevolvingCreditCalculatorTest.php` if a direct-unit test for the guards does not already exist; otherwise fold them into the existing `it_calculates_interest_using_direct_monthly_method` corrected in Task 2.
  </action>
  <acceptance_criteria>
    - `grep -q "use PHPUnit\\\\Framework\\\\Attributes\\\\Test;" tests/Unit/RevolvingCreditCalculatorTest.php` exits 0
    - `php artisan test --filter=RevolvingCreditCalculatorTest 2>&1 | grep -q "passed"` exits 0 (confirms PHPUnit actually discovers and runs tests in this file, not "No tests found")
    - `grep -q "annualRatePercent / 100 / 12" app/Services/RevolvingCreditCalculator.php` exits 0
    - `grep -c "(\$annualRatePercent / 100)" app/Services/RevolvingCreditCalculator.php` returns 0
    - `grep -q "/ 100 / 12" app/Enums/InterestCalculationMethod.php` exits 0
    - `grep -c "case DIRECT_MONTHLY" app/Enums/InterestCalculationMethod.php` returns 1 (the enum case is preserved, not removed)
    - `grep -c "direct_monthly" app/Http/Requests/Api/StoreCreditCardRequest.php` returns 1 (validation untouched)
    - `php artisan test --filter=RevolvingCreditCalculatorTest` runs (failures allowed at this point only in the four stale assertions Task 2 corrects)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=RevolvingCreditCalculatorTest</automated>
  </verify>
  <done>`calculateInterestDirectMonthly` divides the annual rate by 12, the enum description states the corrected formula, both enum cases and all API validation remain intact, and the guard clauses still return 0.0 for non-positive balance or rate.</done>
</task>

<task type="auto">
  <name>Task 2: Correct every stale direct_monthly assertion and strip real-statement provenance from the touched tests</name>
  <files>tests/Unit/RevolvingCreditCalculatorTest.php, tests/Unit/CreditCardDailyBalanceTest.php</files>
  <read_first>
    - tests/Unit/RevolvingCreditCalculatorTest.php lines 1-375 in full (eight affected methods total: `it_calculates_daily_balances_for_a_cycle`, `it_calculates_interest_from_daily_balances`, `first_cycle_has_zero_interest` near the top of the file — unrelated to direct_monthly but sharing the real `542` fixture — plus `it_validates_user_bank_statement_14_percent`, `it_calculates_interest_using_direct_monthly_method`, `it_uses_direct_monthly_method_when_configured`, `daily_balance_and_direct_monthly_produce_different_results`)
    - tests/Unit/CreditCardDailyBalanceTest.php lines 148-165 (`daily_balance_interest_is_lower_than_direct_monthly_rate`)
    - database/factories/CreditCardCycleFactory.php (its default dates are RANDOM — any test asserting an exact daily-balance figure must set `period_start_date` and `statement_date` explicitly)
    - .planning/phases/19-revolving-credit-card-interest-engine-correctness-align-cycl/19-03-SUMMARY.md (confirms the daily-balance opening-balance change from plan 19-03, which these exact figures depend on)
  </read_first>
  <action>
All five methods below currently encode a defect as "expected". Correct them — do NOT weaken assertions into range checks where an exact value is derivable.

Every fixture below replaces the `542.00` balance with a fictional `600.00` and uses fictional 2027 dates. Per D-04, `542`, `75.88`, `1183.30`, `1909.98`, `14.07`, `21.98`, `233.93` and the 2026-03/04/05/06/07 statement dates are real-card figures and must not remain in, or be re-added to, any committed file.

**In `tests/Unit/RevolvingCreditCalculatorTest.php`:**

1. `it_calculates_interest_using_direct_monthly_method` (line ~294):
   - `$currentBalance = 542.00;` → `$currentBalance = 600.00;`
   - comment `// Direct monthly: 542 * (14 / 100) = 75.88` → `// Flat monthly: 600 * (14 / 100 / 12) = 7.00`
   - `assertEqualsWithDelta(75.88, $interest, 0.01)` → `assertSame(7.0, $interest)`
   - Also assert the guard cases in this same method: `assertSame(10.0, $this->calculator->calculateInterestDirectMonthly(1000.00, 12.0))`, `assertSame(0.0, $this->calculator->calculateInterestDirectMonthly(0.0, 14.0))`, `assertSame(0.0, $this->calculator->calculateInterestDirectMonthly(600.00, 0.0))`, `assertSame(0.0, $this->calculator->calculateInterestDirectMonthly(-50.0, 14.0))`

2. `it_uses_direct_monthly_method_when_configured` (line ~307):
   - card `'current_balance' => 542.00` → `600.00`
   - comment `// Should use direct monthly: 542 * 0.14 = 75.88` → `// direct_monthly: 600 * 14 / 100 / 12 = 7.00`
   - `assertEqualsWithDelta(75.88, $breakdown['interest_amount'], 0.01)` → `assertSame(7.0, $breakdown['interest_amount'])`
   - Additionally assert `assertSame(243.0, $breakdown['principal_amount'])` and `assertSame(252.0, $breakdown['total_due'])` (exclusive default: 250 − 7 = 243; 250 + 2 = 252 — confirm the card fixture's `stamp_duty_amount`; if the factory default 2.00 is not in play, set it explicitly to 2)

3. `daily_balance_and_direct_monthly_produce_different_results` (line ~336):
   - card `'current_balance' => 542.00` → `600.00`
   - Give the target cycle EXPLICIT dates so the daily figure is deterministic: `'period_start_date' => Carbon::parse('2027-05-07'), 'statement_date' => Carbon::parse('2027-06-06')` (31 days) alongside the existing `'total_spent' => 0, 'status' => 'open'`
   - Replace the trailing directional assertion. Delete `assertGreaterThan($breakdownDaily['interest_amount'], $breakdownMonthly['interest_amount'])` and the `// Direct monthly should be ~75.88, daily should be much lower (~6.49 for 31 days)` comment — that direction is no longer true and the test must not merely be relaxed. Assert exact values instead:
     - `assertEqualsWithDelta(7.13, $breakdownDaily['interest_amount'], 0.01)`  (31 days × 600 = 18600 balance-days; 18600 × 0.14 / 365 = 7.1342)
     - `assertSame(7.0, $breakdownMonthly['interest_amount'])`
     - keep `assertNotEquals($breakdownDaily['interest_amount'], $breakdownMonthly['interest_amount'])`
     - add the comment `// Daily accrual over a 31-day period and a flat monthly twelfth are close but not identical.`

4. `it_validates_user_bank_statement_14_percent` (line ~183):
   - Rename to `daily_balance_interest_over_a_31_day_cycle_at_14_percent`
   - Delete the entire real-case narrative comment block (lines ~186 and ~208-221, everything mentioning "User's real case", "542 debt", "75.88", "user's bank", "User confirmed", "User will provide more statements")
   - card `'current_balance' => 542.00` → `600.00`; add `'stamp_duty_amount' => 2,` if absent
   - cycle dates `2026-03-21`/`2026-04-20` → `Carbon::parse('2027-05-07')` / `Carbon::parse('2027-06-06')`
   - Replace the two range assertions (`assertGreaterThan(5.0, ...)` / `assertLessThan(8.0, ...)`) with the exact `assertEqualsWithDelta(7.13, $breakdown['interest_amount'], 0.01)` and add `// 31 days × 600 balance-days × 14% / 365 = 7.13`

6. `it_calculates_daily_balances_for_a_cycle` (line ~24) — unrelated to direct_monthly, but hardcodes the real `542` pre-cycle balance:
   - card `'current_balance' => 692.00, // 542 pre-cycle + 150 total_spent (withoutEvents, so manual)` → `'current_balance' => 750.00, // 600 pre-cycle + 150 total_spent (withoutEvents, so manual)`
   - comments `// Starting balance: 542` / `// Mar 1: 542 + 100 = 642` → `// Starting balance: 600` / `// Mar 1: 600 + 100 = 700`
   - `assertEquals(642.00, $dailyBalances['2026-03-01'])` (both occurrences, Mar 1 and Mar 2/Mar 4) → `assertEquals(700.00, ...)`
   - comment `// Mar 5: 642 + 50 = 692` → `// Mar 5: 700 + 50 = 750`
   - `assertEquals(692.00, $dailyBalances['2026-03-05'])` and the Mar 20 assertion → `assertEquals(750.00, ...)`
   - Leave the 2026-03-01/2026-03-20 dates as-is — only the balance figures are real-card provenance, the dates in this method are structurally arbitrary and already appear as plain synthetic test dates elsewhere in the suite

7. `it_calculates_interest_from_daily_balances` (line ~76): the 20-entry `$dailyBalances` array of `542.00` and the `4.16` expected result share the exact math already established in Task 2 item 5 below for `CreditCardDailyBalanceTest.php`'s 20-day case:
   - Replace all 20 array values `'2026-03-0X' => 542.00,` → `'2026-03-0X' => 600.00,` (keep the same 20 date keys, only the value changes)
   - comment `// 20 days at 542 * (14% / 365)` / `// Expected: 542 * 0.14 / 365 * 20 ≈ 4.16` → `// 20 days at 600 * (14% / 365)` / `// Expected: 600 * 0.14 / 365 * 20 ≈ 4.60`
   - `assertEqualsWithDelta(4.16, $interest, 0.01)` → `assertEqualsWithDelta(4.60, $interest, 0.01)`

8. `first_cycle_has_zero_interest` (line ~110): no exact-value assertion depends on the balance (only `isFirstCycle()` truthiness and `assertGreaterThan(0.0, ...)` on the second cycle), so this is a pure substitution:
   - card `'current_balance' => 542.00,` → `'current_balance' => 600.00,`

**In `tests/Unit/CreditCardDailyBalanceTest.php`:**

5. `daily_balance_interest_is_lower_than_direct_monthly_rate` (line ~150):
   - Rename to `daily_balance_interest_over_20_days_is_lower_than_the_flat_monthly_rate`
   - `array_fill_keys(range(1, 20), 542.0)` → `array_fill_keys(range(1, 20), 600.0)`
   - Delete the `// Direct monthly: 542 * 0.14 = 75.88` and `// Daily over 20 days: 542 * ...` comments
   - `assertLessThan(75.88, $dailyInterest)` → compute the comparison from the calculator instead of a literal: `$monthlyInterest = $calculator->calculateInterestDirectMonthly(600.0, 14.0); $this->assertSame(7.0, $monthlyInterest); $this->assertLessThan($monthlyInterest, $dailyInterest);`
   - `assertEqualsWithDelta(4.16, $dailyInterest, 0.01)` → `assertEqualsWithDelta(4.60, $dailyInterest, 0.01)`  (20 × 600 = 12000 balance-days; 12000 × 0.14 / 365 = 4.6027)

After editing, sweep both files: no occurrence of `75.88`, `542`, `4.16`, or any case-insensitive match of `real bank`, `real amex`, `user's card`, `user's real`, `user confirmed`, `bank statement` may remain.
  </action>
  <acceptance_criteria>
    - `grep -c "75.88" tests/Unit/RevolvingCreditCalculatorTest.php tests/Unit/CreditCardDailyBalanceTest.php` returns 0 for both files
    - `grep -c "542" tests/Unit/RevolvingCreditCalculatorTest.php tests/Unit/CreditCardDailyBalanceTest.php` returns 0 for both files
    - `grep -icE "real bank|real amex|user's card|user's real|user confirmed|bank statement" tests/Unit/RevolvingCreditCalculatorTest.php tests/Unit/CreditCardDailyBalanceTest.php` returns 0 for both files
    - `grep -q "assertSame(7.0, \$interest)" tests/Unit/RevolvingCreditCalculatorTest.php` exits 0
    - `grep -q "assertEqualsWithDelta(7.13" tests/Unit/RevolvingCreditCalculatorTest.php` exits 0
    - `grep -q "assertEqualsWithDelta(4.60" tests/Unit/CreditCardDailyBalanceTest.php` exits 0
    - `grep -c "assertGreaterThan(\$breakdownDaily" tests/Unit/RevolvingCreditCalculatorTest.php` returns 0
    - `grep -q "it_validates_user_bank_statement_14_percent" tests/Unit/RevolvingCreditCalculatorTest.php` exits 1 (method renamed)
    - `grep -q "assertEquals(700.00" tests/Unit/RevolvingCreditCalculatorTest.php` exits 0
    - `grep -q "assertEquals(750.00" tests/Unit/RevolvingCreditCalculatorTest.php` exits 0
    - `grep -c "'2026-03-0.' => 542.00" tests/Unit/RevolvingCreditCalculatorTest.php` returns 0
    - `php artisan test --filter=RevolvingCreditCalculatorTest` passes with 0 failures
    - `php artisan test --filter=CreditCardDailyBalanceTest` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=RevolvingCreditCalculatorTest && php artisan test --filter=CreditCardDailyBalanceTest</automated>
  </verify>
  <done>All eight affected test methods (5 direct_monthly-related plus 3 unrelated methods that happened to share the real `542` fixture) assert the corrected synthetic values (7.0 monthly, 7.13 daily over 31 days, 4.60 daily over 20 days, 243.0 principal, 700/750 daily-balance figures), the no-longer-true `monthly > daily` direction assertion is replaced by exact values rather than removed, and no real-statement figure or provenance comment survives in either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card config (`interest_calculation_method`) → interest computation | A user-selectable enum value determines how much interest is billed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering | `direct_monthly` selection producing ~12x the correct charge | mitigate | Formula corrected to `annualRate / 100 / 12`; the corrected value is pinned by an exact `assertSame(7.0, ...)` unit assertion so a regression fails the suite |
| T-19-12 | Tampering | Negative/zero rate or balance passed to `calculateInterestDirectMonthly` | mitigate | Existing `<= 0` guard retained and now explicitly covered by four guard assertions |
| T-19-13 | Repudiation | Enum value silently removed, breaking the documented API contract | accept | Option deliberately retained: both enum cases, `Rule::in([...])` in both request classes, and the Scribe docs are untouched; grep gates assert `DIRECT_MONTHLY` still exists |
| T-19-14 | Information Disclosure | Real statement figures persisting in committed tests | mitigate | D-04: sweep gates assert zero occurrences of `75.88`, `542`, and all provenance phrasings in both touched files |
</threat_model>

<verification>
- `php artisan test --filter=RevolvingCreditCalculatorTest` passes
- `php artisan test --filter=CreditCardDailyBalanceTest` passes
- `php artisan test --testsuite=Unit` passes (any remaining failure must be in `tests/Feature/`, which plan 19-05 owns)
</verification>

<success_criteria>
- `direct_monthly` charges one twelfth of the annual rate per month
- The enum description matches the implemented formula and both enum cases survive
- Zero tests assert the old ~12x figure
- Zero real-statement figures or provenance comments remain in the two touched test files
</success_criteria>

<output>
After completion, create `.planning/phases/19-revolving-credit-card-interest-engine-correctness-align-cycl/19-04-SUMMARY.md`
</output>
