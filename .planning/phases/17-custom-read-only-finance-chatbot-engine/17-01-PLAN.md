---
phase: 17-custom-read-only-finance-chatbot-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/UpcomingPaymentsService.php
  - app/Http/Controllers/Api/V1/DashboardController.php
  - tests/Unit/Services/UpcomingPaymentsServiceTest.php
autonomous: true
requirements: [D-07.2]
must_haves:
  truths:
    - "Upcoming-payments aggregation lives in one place (a service), callable from both the dashboard endpoint and any future consumer"
    - "GET /api/v1/dashboard/upcoming-payments returns byte-identical JSON to before the refactor"
    - "The service enforces the same per-user ownership guards the controller enforced (loan via whereHas, credit card via whereHas, subscription via user_id)"
  artifacts:
    - path: "app/Services/UpcomingPaymentsService.php"
      provides: "forUser(User $user, int $days): array — merged, sorted upcoming payments"
      contains: "public function forUser"
    - path: "tests/Unit/Services/UpcomingPaymentsServiceTest.php"
      provides: "Unit coverage of merge/sort/ownership behavior"
      contains: "UpcomingPaymentsServiceTest"
  key_links:
    - from: "app/Http/Controllers/Api/V1/DashboardController.php"
      to: "app/Services/UpcomingPaymentsService.php"
      via: "constructor injection + forUser() call"
      pattern: "upcomingPaymentsService->forUser"
---

<objective>
Extract the ~85-line upcoming-payments aggregation currently inlined in `DashboardController::upcomingPayments()` into a reusable `App\Services\UpcomingPaymentsService`, so the Phase 17 chatbot intent (plan 17-03) consumes the exact same logic instead of duplicating it.

Purpose: RESEARCH.md "Don't Hand-Roll" and Pitfall 1 identify silent duplication of this aggregation as the top divergence risk for D-07.2 ("reuse the same data the dashboard's upcoming-payments widget already surfaces"). The logic is controller-inline today, so literal reuse is impossible without this extraction.
Output: New service class, thinned controller action, unit test, and a green `DashboardApiTest` proving zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-custom-read-only-finance-chatbot-engine/17-RESEARCH.md
@.planning/phases/17-custom-read-only-finance-chatbot-engine/17-PATTERNS.md

<interfaces>
Existing service the extracted code already depends on (injected in `DashboardController::__construct` today):

```php
// app/Services/SubscriptionService.php
public function getBillingAmount(Subscription $subscription): float;
public function hasPostingForRenewal(Subscription $subscription, $date): bool;
```

Ownership idioms in play (these models do NOT use `HasUserScoping`):
- `LoanPayment` → `whereHas('loan', fn ($q) => $q->where('user_id', $user->id))`
- `CreditCardPayment` → `whereHas('creditCard', fn ($q) => $q->where('user_id', $user->id))`
- `Subscription` → `where('user_id', $user->id)` (carries `user_id` directly)

All three are wrapped in `->when(! $user->hasRole('superadmin'), ...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit test for UpcomingPaymentsService</name>
  <files>tests/Unit/Services/UpcomingPaymentsServiceTest.php</files>
  <read_first>
    - tests/Feature/Api/DashboardApiTest.php (fixture-building conventions: `User::factory()`, `Account::factory()`, `$loan->payments()->create([...])`, `Sanctum::actingAs`)
    - app/Http/Controllers/Api/V1/DashboardController.php (lines 47-136 — the exact logic being extracted)
    - tests/Unit/SubscriptionServiceTest.php (unit-test conventions for services in this repo)
  </read_first>
  <behavior>
    - Test 1 `test_it_returns_pending_loan_payments_within_the_window`: loan payment due in 3 days, `forUser($user, 7)` returns one row with `id === 'loan-'.$payment->id`, `type === 'loan'`, `amount === 250.0`, `transaction_posted === false`.
    - Test 2 `test_it_merges_and_sorts_all_three_payment_sources_by_due_date`: create a subscription renewing in 1 day, a credit-card payment due in 2 days, a loan payment due in 3 days; `forUser($user, 7)` returns 3 rows ordered subscription, credit-card, loan.
    - Test 3 `test_it_excludes_other_users_payments`: a second user's loan payment inside the window is absent from `forUser($user, 7)`.
    - Test 4 `test_it_excludes_paid_payments`: a loan payment with `status = 'paid'` in the window is absent.
  </behavior>
  <action>
Create `tests/Unit/Services/UpcomingPaymentsServiceTest.php` with `declare(strict_types=1);`, namespace `Tests\Unit\Services`, `use Illuminate\Foundation\Testing\RefreshDatabase;`, extending `Tests\TestCase`.

Resolve the service under test with `app(\App\Services\UpcomingPaymentsService::class)`.

Because ownership checks call `$user->hasRole('superadmin')`, authenticate the fixture user with `$this->actingAs($user)` before calling `forUser()` (Spatie roles resolve fine on a plain model, but `Account`/`Transaction` global scopes assume an auth context).

Assert against the returned value as an array of associative arrays (the service returns `array`, NOT a JsonResponse). Use `$result = $service->forUser($user, 7);` then `$this->assertSame('loan-'.$payment->id, $result[0]['id']);` style assertions — do not assert on JSON structure.

This test MUST fail at this point with "Class App\Services\UpcomingPaymentsService does not exist" — that is the expected RED state.
  </action>
  <verify>
    <automated>php artisan test --filter=UpcomingPaymentsServiceTest 2>&1 | grep -q "does not exist\|FAILURES\|Error" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Unit/Services/UpcomingPaymentsServiceTest.php` succeeds
    - `grep -c "public function test_" tests/Unit/Services/UpcomingPaymentsServiceTest.php` returns 4
    - `grep -q "UpcomingPaymentsService::class" tests/Unit/Services/UpcomingPaymentsServiceTest.php` succeeds
    - `php artisan test --filter=UpcomingPaymentsServiceTest` exits non-zero (RED — service does not exist yet)
  </acceptance_criteria>
  <done>Four failing tests exist describing merge, sort, ownership and status filtering for the not-yet-created service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create UpcomingPaymentsService with the extracted aggregation</name>
  <files>app/Services/UpcomingPaymentsService.php</files>
  <read_first>
    - app/Http/Controllers/Api/V1/DashboardController.php (lines 1-136 — source of the logic being moved, verbatim)
    - app/Services/SubscriptionService.php (signatures of `getBillingAmount` / `hasPostingForRenewal`)
    - app/Services/FinanceReportService.php (flat `App\Services` class + method style to mirror)
  </read_first>
  <action>
Create `app/Services/UpcomingPaymentsService.php`:

```php
<?php

namespace App\Services;

use App\Models\CreditCardPayment;
use App\Models\LoanPayment;
use App\Models\Subscription;
use App\Models\User;

class UpcomingPaymentsService
{
    public function __construct(private readonly SubscriptionService $subscriptionService) {}

    /**
     * @return array<int, array<string, mixed>>
     */
    public function forUser(User $user, int $days = 3): array
    {
        // body moved verbatim from DashboardController::upcomingPayments()
    }
}
```

Move the body of `DashboardController::upcomingPayments()` (current lines 54-135) into `forUser()` **verbatim**, with exactly these mechanical substitutions and NO other changes:
- `$days = (int) ($validated['days'] ?? 3);` → use the `$days` method argument (validation stays in the controller).
- `$user = $request->user();` → use the `$user` method argument.
- `$this->subscriptionService->...` stays as-is (the service holds the same injected dependency).
- The final `return response()->json(['data' => collect($loanPayments)->merge(...)->sortBy('due_date')->values()]);` becomes `return collect($loanPayments)->merge($creditCardPayments)->merge($subscriptions)->sortBy('due_date')->values()->all();`

Preserve every field of every mapped row exactly: loan rows keep `id, payment_id, type, description, amount, due_date, status, days_until_due, transaction_posted`; credit-card rows keep the same nine keys; subscription rows keep those nine plus `auto_create_transaction, payment_source_type, posting_target, frequency_label`. Preserve `$today = now()->startOfDay();` and `$until = now()->addDays($days)->endOfDay();`.

Do NOT change any ownership guard: keep all three `->when(! $user->hasRole('superadmin'), ...)` blocks with their existing `whereHas('loan', ...)`, `whereHas('creditCard', ...)`, and `where('user_id', $user->id)` bodies.
  </action>
  <verify>
    <automated>php artisan test --filter=UpcomingPaymentsServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "public function forUser(User \$user, int \$days = 3): array" app/Services/UpcomingPaymentsService.php` succeeds
    - `grep -c "hasRole('superadmin')" app/Services/UpcomingPaymentsService.php` returns 3
    - `grep -q "whereHas('loan'" app/Services/UpcomingPaymentsService.php` and `grep -q "whereHas('creditCard'" app/Services/UpcomingPaymentsService.php` both succeed
    - `grep -q "sortBy('due_date')" app/Services/UpcomingPaymentsService.php` succeeds
    - `grep -q "response()->json" app/Services/UpcomingPaymentsService.php` returns nothing (service returns a plain array)
    - `php artisan test --filter=UpcomingPaymentsServiceTest` exits 0 (all 4 tests GREEN)
  </acceptance_criteria>
  <done>Service exists, returns a plain sorted array, and all four unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Delegate DashboardController::upcomingPayments to the service and prove zero regression</name>
  <files>app/Http/Controllers/Api/V1/DashboardController.php</files>
  <read_first>
    - app/Http/Controllers/Api/V1/DashboardController.php (whole file — you are removing ~85 lines from it)
    - app/Services/UpcomingPaymentsService.php (the service created in Task 2)
    - tests/Feature/Api/DashboardApiTest.php (the regression contract that must stay green)
  </read_first>
  <action>
Rewrite `DashboardController::upcomingPayments()` to exactly:

```php
public function upcomingPayments(Request $request): JsonResponse
{
    $validated = $request->validate([
        'days' => ['nullable', 'integer', 'min:1', 'max:30'],
    ]);

    return response()->json([
        'data' => $this->upcomingPaymentsService->forUser(
            $request->user(),
            (int) ($validated['days'] ?? 3),
        ),
    ]);
}
```

Add the dependency to the existing constructor (currently `public function __construct(private readonly SubscriptionService $subscriptionService) {}`):

```php
public function __construct(
    private readonly SubscriptionService $subscriptionService,
    private readonly UpcomingPaymentsService $upcomingPaymentsService,
) {}
```

Add `use App\Services\UpcomingPaymentsService;` to the import block. Keep `use App\Services\SubscriptionService;` — `$subscriptionService` is still used elsewhere in this controller? Verify with `grep -n "subscriptionService" app/Http/Controllers/Api/V1/DashboardController.php` AFTER the edit: if zero remaining usages, remove both the constructor property and the `use App\Services\SubscriptionService;` import.

Remove the now-unused model imports if and only if no other method in the file references them: check `CreditCardPayment`, `LoanPayment`, `Subscription` with grep before deleting each import. Do NOT remove `Account`, `Transaction`, or `Carbon` (still used by the chart methods).

Do not touch `charts()`, `getMonthlyCashflowChartData()`, `getExpenseCategoriesChartData()`, `getNetWorthTrendChartData()` or the private helpers — the monthly-cashflow helper stays controller-private on purpose: plan 17-04's monthly-spending intent uses the already-extracted, already-tested `FinanceReportService::getTable()` instead, so no second extraction is needed.
  </action>
  <verify>
    <automated>php artisan test --filter=DashboardApiTest && php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "upcomingPaymentsService->forUser" app/Http/Controllers/Api/V1/DashboardController.php` succeeds
    - `grep -c "LoanPayment::query()" app/Http/Controllers/Api/V1/DashboardController.php` returns 0
    - `grep -c "CreditCardPayment::query()" app/Http/Controllers/Api/V1/DashboardController.php` returns 0
    - `php artisan test --filter=DashboardApiTest` exits 0
    - `php artisan test` exits 0 (full suite, no regression anywhere)
  </acceptance_criteria>
  <done>Controller action is under 15 lines, delegates to the service, and both DashboardApiTest and the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/v1/dashboard/upcoming-payments` | Authenticated user supplies `days`; response contains financial obligations |
| service → database | Ownership guards are the only thing preventing cross-user reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | `UpcomingPaymentsService::forUser` | mitigate | Move all three `when(! $user->hasRole('superadmin'), ...)` guards verbatim; `test_it_excludes_other_users_payments` (Task 1) asserts the boundary |
| T-17-02 | Tampering | `days` parameter | mitigate | Validation stays in `DashboardController` (`integer|min:1|max:30`); service signature types `int $days` so no string reaches the query builder |
| T-17-03 | Denial of Service | unbounded date window | mitigate | `max:30` cap retained unchanged; route stays in the `throttle:api-read` group |
</threat_model>

<verification>
- `php artisan test --filter=UpcomingPaymentsServiceTest` — 4 tests green
- `php artisan test --filter=DashboardApiTest` — existing dashboard contract unchanged
- `php artisan test` — full suite green (no cross-domain regression)
- `grep -rn "LoanPayment::query()" app/` returns exactly one hit, inside `app/Services/UpcomingPaymentsService.php` (proves no duplication)
</verification>

<success_criteria>
- `App\Services\UpcomingPaymentsService::forUser(User, int): array` is the single source of upcoming-payments aggregation in the codebase
- `DashboardController::upcomingPayments()` is a thin validate-and-delegate action
- Full PHPUnit suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-custom-read-only-finance-chatbot-engine/17-01-SUMMARY.md`
</output>
