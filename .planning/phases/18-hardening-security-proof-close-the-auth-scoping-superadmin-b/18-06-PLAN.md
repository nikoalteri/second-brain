---
phase: 18-hardening-security-proof-close-the-auth-scoping-superadmin-b
plan: 06
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05"]
files_modified:
  - .planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [D-02, D-05]

must_haves:
  truths:
    - "Every lower-severity finding surfaced by plans 18-01..18-05 is written down with an explicit fix-vs-defer rationale"
    - "CONCERNS.md no longer cites stale 2024-12-19 line numbers for the items this phase re-verified"
    - "CONCERNS.md records which concerns are now closed by a named test"
    - "The full test suite is green at phase close"
  artifacts:
    - path: ".planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md"
      provides: "D-02 lower-severity findings register for Phase 18"
      contains: "Scope decision"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Re-grounded security/known-bugs sections with 2026-08-06 verification status"
      contains: "2026-08-06"
  key_links:
    - from: ".planning/codebase/CONCERNS.md"
      to: "tests/Feature/ScopingSecurityTest.php"
      via: "closed-by-test annotation on the withoutGlobalScopes concern"
      pattern: "ScopingSecurityTest"
    - from: ".planning/phases/18-.../deferred-items.md"
      to: "18-01..18-05 SUMMARY files"
      via: "each entry cites the plan that surfaced it"
      pattern: "18-0"
---

<objective>
Close Phase 18 by discharging D-02's documentation half and re-grounding the stale codebase concern map.

Purpose: D-02 requires that lower-severity findings be **documented rather than fixed**, mirroring Phase 17's `deferred-items.md` convention. Plans 18-01..18-05 each surface findings in their SUMMARY files; without this plan those findings evaporate. Separately, `.planning/codebase/CONCERNS.md` is dated 2024-12-19 and was verified partially stale during this phase's research (wrong line numbers, an incorrect count of `withoutGlobalScopes()` sites, and a missing second observer instance). Leaving it stale means the next phase re-derives scope from bad data — exactly the failure mode Pitfall 1 warns about.

Runs last because its entire input is the set of findings produced by the five preceding plans.

Output: A Phase 18 `deferred-items.md`, a re-grounded `CONCERNS.md`, and a green full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-CONTEXT.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-RESEARCH.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-01-SUMMARY.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-02-SUMMARY.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-03-SUMMARY.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-04-SUMMARY.md
@.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-05-SUMMARY.md

<interfaces>
The established `deferred-items.md` convention — `.planning/phases/17-custom-read-only-finance-chatbot-engine/deferred-items.md`:
```markdown
# Deferred Items — Phase 17

## Pre-existing failing test: FinanceReportPageTest (out of scope)

- **Test:** `Tests\Feature\Filament\FinanceReportPageTest::admin_finance_report_renders_...`
- **Failure:** `assertSee('exceeded')` fails — ...
- **Confirmed pre-existing:** Reproduced on `HEAD` before any 17-01/17-02 changes (via `git stash`) ...
- **Scope decision:** Not touched by phase 17 ...
```
Bold-label bullet list, one `##` section per item, always ending in a `**Scope decision:**` line.

Corrections this phase verified against current code (2026-08-06) that CONCERNS.md must absorb:

| CONCERNS.md claim (2024-12-19) | Verified status 2026-08-06 | Correct location |
|---|---|---|
| `withoutGlobalScopes()` site inventory | 13 sites in `app/` (CONTEXT.md said 14 — one match was a comment line) | see `grep -rn "withoutGlobalScopes" app/` |
| 7 Filament resources bypass user scoping | They pass `withoutGlobalScopes([SoftDeletingScope::class])` — the `user` scope still applies. Only `AccountsResource` overrides `getEloquentQuery()`; the other 6 have no override at all (RESEARCH.md Pitfall 2 wrongly claimed all 7 did) | `app/Filament/Resources/*/*Resource.php` |
| `CreditCardExpenseObserver` static-state leak | Still present | `app/Observers/CreditCardExpenseObserver.php:15` (`$originalPointers`) |
| (absent from the doc) | Second instance of the same pattern | `app/Observers/CreditCardPaymentObserver.php:16` (`$previousStatuses`) |
| Race condition at "lines 164-166, 256-267, 313-322" | Still real; file is now 385 lines | `syncIssuedCycle()` 139-195 (decision 160-166), `syncCycleAndCardFromPayment()` 236-291 (decision 253-267), `handleDeletedPayment()` 293-338 (decision 310-322) |
| Missing validation on CreditCardExpense move | Confirmed via a different mechanism: fail-open early return | `app/Services/CreditCardExpenseService.php:32-34` |
| Transaction Observer recursion on transfer deletion | Guard present; residual risk is that the pair delete is not wrapped in `DB::transaction()` | `app/Observers/TransactionObserver.php:47-58` |

Known candidates for the deferred register (confirm each against the actual SUMMARYs before writing — do not copy this list blindly):
- `TransactionObserver` transfer-pair deletion not wrapped in `DB::transaction()` (RESEARCH.md Open Question 2) — deliberately NOT fixed in this phase
- `CreditCardCycleService::handleDeletedPayment()` still using the delta-based balance pattern that 18-04 replaced in `syncCycleAndCardFromPayment()` only
- `CreditCardCycleService::refreshCycleStatuses()` (lines 340-368) being a non-transactional batch reconciliation loop
- Observer static-state, if plan 18-05 took Branch B
- Any pre-existing failing tests encountered (Phase 17 already logged `FinanceReportPageTest`, `SecurityChecklistTest` attribute-import discovery gap, and the Vite-manifest worktree gap — do not re-litigate those, but note if they resurfaced)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 18 deferred-items register</name>
  <files>.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md</files>
  <read_first>
    - .planning/phases/17-custom-read-only-finance-chatbot-engine/deferred-items.md (the exact format to mirror)
    - .planning/phases/18-.../18-01-SUMMARY.md through 18-05-SUMMARY.md (ALL five — the actual findings; this task has no other legitimate input)
    - .planning/phases/18-.../18-CONTEXT.md (the D-02 severity wording that each disposition must cite)
  </read_first>
  <action>
Create `.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md` titled `# Deferred Items — Phase 18`.

Read all five SUMMARY files first and extract every finding that was surfaced but NOT fixed. Write one `##` section per item, mirroring Phase 17's bullet structure exactly:

```markdown
## <Short finding title> (<why deferred>)

- **Finding:** <what is actually wrong, with the exact file:line as of 2026-08-06>
- **Surfaced by:** <plan id, e.g. 18-04 Task 2> — <the concrete evidence, e.g. the test output or code read>
- **Severity assessment (D-02):** <lower severity, because: no cross-user data disclosure, no corrupted/drifted balances, no broken money math — state which of these it fails to meet>
- **Scope decision:** Documented, not fixed in Phase 18. <what would need to be true to promote it>
```

Mandatory sections (write each only if the corresponding SUMMARY confirms it was left unfixed — otherwise write a one-line note that it was fixed and in which plan):

1. **`TransactionObserver` transfer-pair deletion not transaction-wrapped** — `app/Observers/TransactionObserver.php:47-58` deletes the paired transfer transaction outside any `DB::transaction()`. Note explicitly that RESEARCH.md Open Question 2 recommended fixing it but the phase's fix bucket was scoped to the three confirmed high-severity items (TransactionCategories leak, credit-card balance drift, expense validation fail-open), and that promoting this requires reproducing an actual orphaned pair — which no test in this phase achieved.
2. **`CreditCardCycleService::handleDeletedPayment()` delta-based balance pattern** — lines 293-338 retain the `applyPrincipalPayment`/`reversePrincipalPayment` delta form that 18-04 replaced in `syncCycleAndCardFromPayment()`. State whether 18-04's SUMMARY recorded it as reachable by the same race.
3. **`CreditCardCycleService::refreshCycleStatuses()` non-transactional batch loop** — lines 340-368; individual `$cycle->update()` calls are atomic per row but the loop is not.
4. **Observer static-state** — include only if 18-05 took Branch B; cite the measured `$residue` value from `ObserverStaticStateTest`.
5. **Any leak or failure surfaced by 18-02 or 18-03 that was not fixed** — e.g. a `HasUserScoping` model that leaked, or the ambient-authentication console test failing. If any of these occurred and were NOT fixed, this section must state why, since a cross-user leak is high severity by D-02 and deferring one requires explicit justification.

At the end add:
```markdown
## Not deferred — fixed in this phase

| Finding | Fixed by | Regression test |
|---------|----------|-----------------|
| ... | 18-0N | `tests/...::test_...` |
```
listing every high-severity item this phase actually closed, with its regression test path.

Do not invent findings. Every entry must trace to a SUMMARY. If a SUMMARY is missing or empty, say so explicitly in the register rather than fabricating content.
  </action>
  <acceptance_criteria>
    - `.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md` exists and its first line is `# Deferred Items — Phase 18`
    - `grep -c "\*\*Scope decision:\*\*" .planning/phases/18-*/deferred-items.md` returns 1 or more
    - `grep -c "\*\*Severity assessment (D-02):\*\*" .planning/phases/18-*/deferred-items.md` returns 1 or more
    - `grep -c "TransactionObserver" .planning/phases/18-*/deferred-items.md` returns 1 or more
    - `grep -c "handleDeletedPayment" .planning/phases/18-*/deferred-items.md` returns 1 or more
    - `grep -c "Not deferred — fixed in this phase" .planning/phases/18-*/deferred-items.md` returns `1`
    - Every `**Surfaced by:**` line names a plan id matching `18-0[1-5]`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md && grep -c "Scope decision" .planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md</automated>
  </verify>
  <done>Every unfixed Phase 18 finding is registered with a D-02 severity rationale traceable to a plan SUMMARY, and every fixed finding is listed with its regression test.</done>
</task>

<task type="auto">
  <name>Task 2: Re-ground CONCERNS.md against verified 2026-08-06 code and run the phase gate</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (whole file — the Security Considerations and Known Bugs sections in particular)
    - .planning/phases/18-.../18-RESEARCH.md "State of the Art" and "Re-mapped race-condition line numbers" sections (the verified corrections)
    - .planning/phases/18-.../deferred-items.md (Task 1 output — the closed/deferred split must agree between the two documents)
    - All five 18-0N-SUMMARY.md files (what actually changed, so "closed by" claims are accurate)
  </read_first>
  <action>
Edit `.planning/codebase/CONCERNS.md` in place. Do not rewrite unrelated sections (Tech Debt entries about the deprecated `calculateRevolvingPaymentBreakdown()`, the service-locator anti-pattern, and the magic interest rate stay untouched).

1. Update the header. Current line 3 reads `**Analysis Date:** 2024-12-19`. Replace with:
```markdown
**Analysis Date:** 2024-12-19
**Partially re-verified:** 2026-08-06 (Phase 18 — Security Considerations and Known Bugs sections only; Tech Debt entries were NOT re-verified and may still be stale)
```

2. In the **Security Considerations** section, for each `withoutGlobalScopes()` / `HasUserScoping` concern, append a verification line using this exact shape:
```markdown
- **Status 2026-08-06 (Phase 18):** <CLOSED|OPEN|CORRECTED> — <what changed> — covered by `<test file>::<test method>`
```
Required content:
- Correct the site inventory to **13** call sites in `app/` and note CONTEXT.md's 14 counted a comment line.
- Correct the Filament claim: all 7 pass `withoutGlobalScopes([SoftDeletingScope::class])` in `getRecordRouteBindingEloquentQuery()`, so the `user` global scope still applies; only `AccountsResource` overrides `getEloquentQuery()`. Cross-user record binding is now asserted by `tests/Feature/Filament/AdminPanelScopingTest.php`.
- Mark the `TransactionCategories.php` ungated query as **CLOSED** — fixed in 18-01, covered by `tests/Feature/Api/GraphQLApiTest.php::test_graphql_transaction_categories_query_returns_only_own_categories`.
- Mark the generic tenant-boundary concern as **CLOSED** — covered by `tests/Feature/ScopingSecurityTest.php`.
- Mark the `HasUserScoping` non-HTTP auth-context concern as **CLOSED** — covered by `tests/Feature/ConsoleScopingTest.php`; state that the no-op is intentional and now contractually asserted.
Adjust each verdict to match what the SUMMARYs actually report — if a plan recorded a failure, write OPEN, not CLOSED.

3. In the **Known Bugs** section:
- Replace the stale race-condition line numbers with the verified mapping: `app/Services/CreditCardCycleService.php` is 385 lines; `syncIssuedCycle()` 139-195 (status decision 160-166), `syncCycleAndCardFromPayment()` 236-291 (status decision 253-267), `handleDeletedPayment()` 293-338 (status decision 310-322), `refreshCycleStatuses()` 340-368.
- Add the Phase 18 status line for the race condition reflecting 18-04's outcome (the balance half of `syncCycleAndCardFromPayment()` now recomputes authoritatively via `syncCardBalance()`; `handleDeletedPayment()` unchanged and deferred).
- Correct the `CreditCardExpense` move-validation entry to the actual mechanism (fail-open early return at `app/Services/CreditCardExpenseService.php:32-34`) and mark its Phase 18 status per 18-05.
- Correct the observer static-state entry: still present at `app/Observers/CreditCardExpenseObserver.php:15`, and add the previously undocumented second instance at `app/Observers/CreditCardPaymentObserver.php:16`. Add the note that no Octane/Swoole is in use (`composer.json` verified), so the risk is intra-request only, and cite `tests/Unit/Observers/ObserverStaticStateTest.php`.
- Correct the `TransactionObserver` entry to the verified location `app/Observers/TransactionObserver.php:47-58` and mark it OPEN, cross-referencing `deferred-items.md`.

4. Add a short closing subsection:
```markdown
### Phase 18 verification note

Line numbers in the Security Considerations and Known Bugs sections above were re-verified against the working tree on 2026-08-06. Line numbers elsewhere in this document are from 2024-12-19 and must be re-grepped before use. Unfixed Phase 18 findings and their severity rationale live in `.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/deferred-items.md`.
```

5. Run the phase gate: `php artisan test` (full suite). Record the exact tests/assertions counts in the plan SUMMARY. The suite must be green. If it is not, do NOT paper over it in CONCERNS.md — report the failure and stop.
  </action>
  <acceptance_criteria>
    - `grep -c "2026-08-06" .planning/codebase/CONCERNS.md` returns 5 or more
    - `grep -c "Partially re-verified" .planning/codebase/CONCERNS.md` returns `1`
    - `grep -c "ScopingSecurityTest" .planning/codebase/CONCERNS.md` returns 1 or more
    - `grep -c "ConsoleScopingTest" .planning/codebase/CONCERNS.md` returns 1 or more
    - `grep -c "AdminPanelScopingTest" .planning/codebase/CONCERNS.md` returns 1 or more
    - `grep -c "ObserverStaticStateTest" .planning/codebase/CONCERNS.md` returns 1 or more
    - `grep -c "CreditCardPaymentObserver.php:16" .planning/codebase/CONCERNS.md` returns 1 or more
    - `grep -c "164-166" .planning/codebase/CONCERNS.md` — if non-zero, each occurrence sits inside the corrected mapping for `syncIssuedCycle()` lines 139-195, not as a standalone stale citation
    - `grep -c "deferred-items.md" .planning/codebase/CONCERNS.md` returns 1 or more
    - `php artisan test` exits 0, and its final "Tests: N passed" line is quoted verbatim in the plan SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>php artisan test</automated>
  </verify>
  <done>CONCERNS.md's security and known-bugs sections carry accurate 2026-08-06 locations, per-concern CLOSED/OPEN verdicts tied to named tests, and a pointer to the deferred register; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → future phase scoping | Stale or over-optimistic concern documentation causes the next phase to skip a real vulnerability or re-derive scope from wrong line numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Repudiation (lost finding) | Findings surfaced in plan SUMMARYs but never registered | mitigate | `deferred-items.md` requires every entry to cite the surfacing plan id and a D-02 severity rationale (Task 1) |
| T-18-06-02 | Tampering (misleading documentation) | `CONCERNS.md` marking a concern CLOSED without a real test | mitigate | Every CLOSED verdict must name a concrete `<file>::<method>` and match what the SUMMARY reports; acceptance criteria grep for the four new test class names (Task 2) |
| T-18-06-03 | Information Disclosure | None — this plan touches only planning documents | accept | No application code or runtime surface is modified |
| T-18-06-04 | Repudiation | Stale 2024-12-19 line numbers reused by a future phase | mitigate | Explicit "Partially re-verified" header plus a closing note stating which sections are trustworthy and which must be re-grepped (Task 2) |
</threat_model>

<verification>
1. `test -f .planning/phases/18-*/deferred-items.md` and every `**Surfaced by:**` line names an `18-0[1-5]` plan.
2. `grep -c "2026-08-06" .planning/codebase/CONCERNS.md` returns 5 or more.
3. The CLOSED/OPEN verdicts in CONCERNS.md agree item-for-item with `deferred-items.md` (no item marked CLOSED in one and deferred in the other).
4. `php artisan test` exits 0 with the count quoted in the SUMMARY.
</verification>

<success_criteria>
- D-02's documentation half is discharged: every lower-severity finding is written down with a severity rationale and a promotion condition.
- CONCERNS.md's security and known-bugs sections are accurate as of 2026-08-06, including the two corrections this phase found in RESEARCH.md itself (the Filament `getEloquentQuery()` claim and the site count).
- The two documents do not contradict each other.
- Full test suite green at phase close.
</success_criteria>

<output>
After completion, create `.planning/phases/18-hardening-security-proof-close-the-auth-scoping-superadmin-b/18-06-SUMMARY.md`
</output>
