# External Integrations

**Analysis Date:** 2026-03-23

## APIs & External Services

**GraphQL API:**
- GraphQL server (Lighthouse) - Primary API for frontend/external clients
  - SDK/Client: `nuwave/lighthouse` (server-side), axios via frontend
  - Schema location: `graphql/schema.graphql`
  - Middleware: API rate limiting via `App\Http\Middleware\ApiRateLimitMiddleware`

**No detected third-party APIs** - Application is self-contained with no external service integrations (no Stripe, AWS, Supabase, etc.)

## Data Storage

**Databases:**
- SQLite (default, file-based)
  - Connection: `DB_CONNECTION=sqlite` in `.env`
  - Client: Laravel Eloquent ORM
  - Alternative: MySQL or PostgreSQL (configured in `config/database.php`)

**File Storage:**
- Local filesystem only (no cloud storage integration detected)
- Storage location: `storage/` directory with standard Laravel structure

**Caching:**
- Not detected - Default Laravel cache configuration without Redis/Memcached

## Authentication & Identity

**Auth Provider:**
- Custom Laravel authentication
  - Implementation: Laravel Sanctum (token-based API auth) + session-based admin auth
  - User model: `App\Models\User`
  - Guards: web (Filament dashboard), api (GraphQL/REST endpoints)

**Authorization:**
- Role-Based Access Control (RBAC) via Spatie Permission package
- Policies: `app/Policies/` directory contains authorization logic
- Middleware: Request validation and authentication checks

## Monitoring & Observability

**Error Tracking:**
- Not detected - No external error tracking service (Sentry, Bugsnag, etc.)

**Logs:**
- Laravel default logging (stack driver) to `storage/logs/`
- Optional pail integration: `laravel/pail` for CLI log viewing

## CI/CD & Deployment

**Hosting:**
- Not specified - Ready for any standard PHP hosting (Laravel-compatible)

**CI Pipeline:**
- Not detected - No GitHub Actions, GitLab CI, or similar configured

**Local Development:**
- Laravel Sail available for Docker-based development
- Development server: `php artisan serve`
- Queue worker: `php artisan queue:listen`
- Asset bundler: `npm run dev` (Vite)

## Environment Configuration

**Required env vars:**
- `APP_NAME` - Application name
- `APP_ENV` - Environment (local/production)
- `APP_KEY` - Application encryption key (generated by `php artisan key:generate`)
- `APP_URL` - Application URL
- `DB_CONNECTION` - Database driver (sqlite/mysql/pgsql)
- `DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD` - Database credentials

**Secrets location:**
- `.env` file (local, not committed)
- Secured in deployment platform (environment variables)

## Webhooks & Callbacks

**Incoming:**
- Not detected

**Outgoing:**
- Not detected

## Job Queue

**Configuration:**
- Laravel queue framework for background jobs
- Default: `sync` driver (jobs execute immediately)
- Job location: `app/Jobs/`
- Example: `GenerateLoanPaymentsJob` - processes loan payment generation asynchronously

## Background Processes

**Database Migrations:**
- Located in `database/migrations/`
- Seeders: `database/seeders/` for test data

**Event Broadcasting:**
- Not detected - No WebSocket or real-time event integration

---

*Integration audit: 2026-03-23*
