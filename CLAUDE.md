# Project Instructions

## Project Knowledge

This project has a Graphify knowledge graph at:

- `graphify-out/`

A generated Obsidian knowledge base is available at:

- `~/Documents/DevKnowledge/second-brain`

Use Graphify and the Obsidian knowledge base when investigating:
- architecture
- dependencies
- call flows
- relationships between components
- unfamiliar areas of the codebase

Prefer consulting relevant knowledge-base notes before performing broad source-code searches.

Do not load or scan the entire Obsidian knowledge base unnecessarily. Read only the notes relevant to the current task.

The current source code is always the source of truth. If generated knowledge conflicts with the repository, trust the repository.

Note: `.graphifyignore` excludes `tmp/pdfs/` and `.planning/*.pdf`/`.planning/Screenshot*.png` because they contain real personal financial statements used as sample data — never feed those into graphify or any other tool that would extract or publish their contents.
